{
  "name": "parcel",
  "version": "1.0.0",
  "description": "messenger app",
  "scripts": {
    "start": "parcel build ./static/pages/**/*.html && node server.js",
    "watch": "parcel watch ./static/pages/**/*.html",
    "production": "parcel build ./static/pages/**/*.html",
    "dev": "npm run watch & node server.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@parcel/transformer-sass": "^2.0.1",
    "parcel": "latest",
    "parcel-transformer-pug-precompile": "^1.0.6",
    "stylelint": "^14.1.0",
    "stylelint-config-standard-scss": "^3.0.0"
  },
  "dependencies": {
    "express": "^4.17.1"
  }
}
